class NumbersModel {
  final String image, title, subtitle, audio;
  const NumbersModel({
    required this.audio,
    required this.image,
    required this.title,
    required this.subtitle,
  });
}